# This application used Flask, elasticsearch, mongodb.
### Just for fun.
  A simple example to show how to make Flask intergate with elasticsearch and MongoDB.
  Used scrapy to crawl the data and used MongoDB to store.

